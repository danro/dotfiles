# first, run

sudo rm -f /private/var/log/asl/*

# then add these lines to /etc/asl.conf

= archive 0
= utmp_ttl 604800
= fs_ttl 604800


# also remove files in /etc/paths.d and convert to $PATH in profile.

PATH=
/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin:/usr/local/git/bin:/usr/local/mongodb/bin:/usr/X11/bin

